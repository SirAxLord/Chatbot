services:
  rasa:
    build: .
    ports:
      - "5005:5005"
    volumes:
      - ./:/app
    command: ["rasa", "run", "--enable-api", "--cors", "*"]

  actions:
    build: .
    volumes:
      - ./:/app
    command: ["rasa", "run", "actions"]
    ports:
      - "5055:5055"